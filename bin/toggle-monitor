#!/usr/bin/env sh

# Usage: toggle-monitor

in="LVDS1"
ext="VGA1"

if (xrandr | grep "$ext disconnected" > /dev/null); then
  xrandr --output "$ext" --off --output "$in" --auto
else
  xrandr --output "$in" --off --output "$ext" --auto
fi
